import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ambalavanan.portfolio',
  appName: 'Ambalavanan Portfolio',
  webDir: 'dist'
};

export default config;
